<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ================================================================
         PERMISSIONS — Android 14/15/16 Compatible
         ================================================================ -->

    <!-- Wake lock to keep CPU running when app backgrounded -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Foreground service — required for background Linux process -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14+ requires specific foreground service type -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Network access for apt-get inside Ubuntu -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Notification permission — REQUIRED to request at runtime on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Boot receiver for auto-start feature -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Battery optimization exemption request -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- External storage — access and create /sdcard/MobileLinux in File Manager -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />

    <!-- Camera & Audio/Microphone permissions for OpenCV, computer vision, AI models, audio processing & WebRTC -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Features declared as optional so devices without physical camera/mic can still install -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:name=".MobileLinuxApp"
        android:allowBackup="false"
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileLinux"
        android:largeHeap="true"
        android:hardwareAccelerated="true"
        tools:targetApi="35">

        <!-- ================================================================
             ACTIVITIES
             ================================================================ -->

        <!-- First-run Setup Wizard -->
        <activity
            android:name=".ui.SetupActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.MobileLinux" />

        <!-- Main Terminal Activity — launcher entry point -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:screenOrientation="unspecified"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|smallestScreenSize|orientation|uiMode"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Settings Activity -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Libraries & Packages Hub Activity -->
        <activity
            android:name=".ui.LibrariesActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Integrated Chromium Developer Browser -->
        <activity
            android:name=".ui.DevBrowserActivity"
            android:exported="true"
            android:screenOrientation="unspecified"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|smallestScreenSize|orientation|uiMode"
            android:launchMode="singleTop"
            android:theme="@style/Theme.MobileLinux">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" android:host="localhost" />
                <data android:scheme="http" android:host="127.0.0.1" />
                <data android:scheme="https" android:host="localhost" />
                <data android:scheme="https" android:host="127.0.0.1" />
            </intent-filter>
        </activity>

        <!-- Native Mobile Code IDE (Acode style touch editor + Linux PRoot runner) -->
        <activity
            android:name=".ui.CodeIdeActivity"
            android:exported="false"
            android:screenOrientation="unspecified"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|smallestScreenSize|orientation|uiMode"
            android:launchMode="singleTop"
            android:theme="@style/Theme.MobileLinux"
            android:hardwareAccelerated="true" />

        <!-- Fullscreen Graphical Linux Desktop (XFCE4 & TigerVNC) -->
        <activity
            android:name=".ui.DesktopActivity"
            android:exported="false"
            android:screenOrientation="sensorLandscape"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|smallestScreenSize|orientation|uiMode"
            android:theme="@style/Theme.MobileLinux"
            android:hardwareAccelerated="true" />

        <!-- ================================================================
             BACKGROUND SERVICE — Android 14/15 Compatible
             ================================================================ -->
        <service
            android:name=".service.LinuxService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <!--
                Android 14+ requires specifying the use case for specialUse.
                This tag explains why specialUse is needed:
                Running a Linux environment (proot Ubuntu) continuously.
            -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Runs Ubuntu Linux environment (proot) for developer terminal use" />
        </service>

        <!-- Boot receiver — disabled by default, user enables in Settings -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true"
            android:enabled="false">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- FileProvider for sharing files from Ubuntu environment -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
