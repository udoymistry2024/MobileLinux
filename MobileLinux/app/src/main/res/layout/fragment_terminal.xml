<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/terminal_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/terminal_bg">

    <!-- Terminal View (fills all space above extra keys) -->
    <com.mobilelinux.terminal.TerminalView
        android:id="@+id/terminal_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/terminal_bg"
        android:focusable="true"
        android:focusableInTouchMode="true" />

    <!-- Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/divider_color" />

    <!-- Extra Keys Container: holds 44dp bar while padding from Android navigation bar / soft keyboard -->
    <FrameLayout
        android:id="@+id/extra_keys_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/extra_keys_bg">

        <com.mobilelinux.ui.ExtraKeysView
            android:id="@+id/extra_keys_view"
            android:layout_width="match_parent"
            android:layout_height="44dp" />
    </FrameLayout>

</LinearLayout>
